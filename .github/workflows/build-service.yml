name: Build and optionally test a service

on:
  workflow_call:
    inputs:
      service-name:
        required: true
        type: string
      build-type:
        required: true
        type: string
        default: 'debug'
      run-tests:
        required: false
        type: boolean
        default: true
      run-in-docker:
        required: false
        type: boolean
        default: true
      upload-artifact:
        required: false
        type: string
        default: ""

env:
  UBSAN_OPTIONS: print_stacktrace=1
  USERVER_STACK_USAGE_MONITOR: false # otherwise userver breaks CI with SIGABRT

jobs:
  build-test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ inputs.service-name }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-${{ inputs.service-name }}-

      - name: Cache ccache directory
        uses: actions/cache@v4
        with:
          path: ${{ inputs.service-name }}/.ccache
          key: ccache-${{ inputs.service-name }}-${{ inputs.build-type }}-${{ github.ref }}-${{ github.run_number }}
          restore-keys: |
            ccache-${{ inputs.service-name }}-${{ inputs.build-type }}-${{ github.ref }}-
            ccache-${{ inputs.service-name }}-${{ inputs.build-type }}-

      - name: Fix run_as_user.sh permissions
        if: inputs.run-in-docker
        run: |
          [ -f run_as_user.sh ] && chmod +x run_as_user.sh || true
        working-directory: ${{ inputs.service-name }}

      - name: Clean build dir
        run: |
           rm -rf build-${{ inputs.build-type }} || true
        working-directory: ${{ inputs.service-name }}

      - name: CMake
        run: make ${{ inputs.run-in-docker && 'docker-' || '' }}cmake-${{ inputs.build-type }}
        working-directory: ${{ inputs.service-name }}

      - name: Build
        run: make ${{ inputs.run-in-docker && 'docker-' || '' }}build-${{ inputs.build-type }}
        working-directory: ${{ inputs.service-name }}

      - name: Test
        if: inputs.run-tests && inputs.build-type == 'debug'
        run: make ${{ inputs.run-in-docker && 'docker-' || '' }}test-${{ inputs.build-type }} \
          DOCKER_EXTRA_OPTS="--security-opt seccomp=unconfined" # runs container in privileged mode
          # because otherwise StackUsageMonitor causes userver to SIGABRT in CI builds
        working-directory: ${{ inputs.service-name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: inputs.upload-artifact != ''
        with:
          name: ${{ inputs.service-name }}-binary
          path: ${{ inputs.service-name }}/build-${{ inputs.build-type }}/${{ inputs.upload-artifact }}